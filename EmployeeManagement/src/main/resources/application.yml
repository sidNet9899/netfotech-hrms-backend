server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://localhost:3308/hrms_employeemanagement
    username: root
    password: Sid9899@00
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

debug: true

application:
  name: EMPLOYEE-MANAGEMENT-SERVICE

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
